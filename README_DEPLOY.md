## 📦 README_DEPLOY.md — Інструкція по розгортанню Telegram-бота `@Oleksii_Profit_bot`

### 🔧 Вимоги

* Сервер Ubuntu 22.04 (наприклад, DigitalOcean Droplet)
* Python 3.10+
* Встановлені пакети: `python3-pip`, `git`, `systemd`, `virtualenv`
* Активні API-ключі:
  - Binance API + Secret
  - Telegram Bot Token
  - OpenAI GPT-ключ

---

### 📁 Структура проєкту

```
telegram-crypto-bot-github/
├── main.py                     # Telegram-бот (GPT, Binance, Telegram)
├── daily_analysis.py           # GPT-аналітика ринку (щоденна)
├── .env                        # Реальні змінні середовища (не пушити!)
├── .env.example                # Шаблон змінних для репозиторію
├── requirements.txt            # Python-залежності
├── .github/workflows/daily.yml # GitHub Actions для auto-звіту
├── README_DEPLOY.md            # Цей файл
---

### ⚙️ Налаштування `.env`

```env
TELEGRAM_TOKEN=токен_бота
ADMIN_CHAT_ID=465786073
OPENAI_API_KEY=sk-...
BINANCE_API_KEY=...
BINANCE_SECRET_KEY=...
```

Файл `.env` повинен лежати в корені проєкту поруч з `main.py`.

---

### 🧠 GPT-логіка

* Щодня о **09:00** і **20:00** GPT-бот запускає `daily_analysis.py`.
* Бот аналізує:
  - поточний Binance-баланс;
  - whitelist монет;
  - динаміку ринку;
  - рекомендації, що купити/продати;
  - стоп-лоси, тейк-профіти, очікуваний прибуток.

* Користувач отримує Telegram-звіт з командами:
  - `/confirmbuyXXX`
  - `/confirmsellXXX`

* Після підтвердження бот **автоматично** виконує угоди через Binance API.

---

### 📊 Команди Telegram-бота

| Команда             | Опис                                                                 |
|---------------------|----------------------------------------------------------------------|
| `/start`            | Старт бота, меню                                                      |
| `/menu`             | Клавіатура з кнопками                                                 |
| `/report`           | GPT-звіт: баланс, сигнали, прибутки                                   |
| `/balance`          | Поточний баланс з Binance                                             |
| `/set_budget`       | Встановити бюджет у USDT                                              |
| `/confirmbuyXXX`    | Підтвердити купівлю монети (автоматична дія)                          |
| `/confirmsellXXX`   | Підтвердити продаж монети (автоматична дія)                           |
| `/status`           | Статус системи                                                        |
| `/buy`              | Ручна купівля                                                          |
| `/sell`             | Ручний продаж                                                         |
| `/help`             | Інструкції                                                             |
| `/history`          | Історія угод                                                           |

---

### 🔐 GitHub Secrets

Для роботи GitHub Actions заповни такі secrets:

- `TELEGRAM_TOKEN`
- `OPENAI_API_KEY`
- `BINANCE_API_KEY`
- `BINANCE_SECRET_KEY`
- `ADMIN_CHAT_ID`

---

### 🧠 GPT-ФУНКЦІЇ

GPT-асистент автоматично:

1. Щодня о **09:00 та 20:00** аналізує:
   - Binance-баланс
   - whitelist монети (30+ пар)
   - ринок Binance (зміна % та обʼєм)
2. Формує рекомендації:
   - що продавати (якщо прогноз мінімальний)
   - що купувати (якщо очікується прибуток)
   - стоп-лос, тейк-профіт, прогноз доходу
3. Генерує звіт у форматі markdown:
   - кількість монет
   - вартість у USDT та UAH
   - PNL (прибуток за день у %)
   - команди для підтвердження дій
4. Очікує підтвердження від користувача.
5. Після підтвердження — **виконує операцію на Binance автоматично**.

---

---

### ✅ Команди Telegram-бота

- `/start` — запуск бота.
- `/menu` — головне меню з кнопками.
- `/report` — GPT-звіт: баланс, сигнали, прогноз.
- `/balance` — поточний баланс гаманця Binance.
- `/confirm_buy` — підтвердження купівлі рекомендованого активу.
- `/confirm_sell` — підтвердження продажу активу.
- `/history` — історія сигналів/операцій.
- `/set_budget` — встановити торговий бюджет.
- `/set_pair` — задати whitelist або дозволені пари.
- `/status` — перевірка статусу.
- `/help` — інструкція.

---

### 📌 Структура GPT-звіту

1. **Баланс:**
   - BTC: 0.0025 ≈ 168 USDT ≈ 7,200 грн
   - ETH: 0.05 ≈ 182 USDT ≈ 7,800 грн
2. **Що продавати:**
   - XRP — низький об’єм, просідання -3.5% за добу.
3. **Що купити:**
   - ARB — зростання +12%, високий об’єм, потенціал +7.2% за добу.
4. **Стоп-лос:** -5%, **Тейк-профіт:** +7%
5. **Очікуваний прибуток:** +13.5 USDT
6. **Команди:**
   - `/confirm_buy_ARB`
   - `/confirm_sell_XRP`

---

